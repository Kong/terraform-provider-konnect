name: Lint
on: [pull_request]
jobs:
  spectral:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: stoplightio/spectral-action@latest
        with:
          file_glob: 'definitions/**/openapi.yaml'