// Package shared is the SDK-side half of the dynamictype golden fixture (mirrors
// internal/sdk/models/shared). It exercises both SDK field shapes:
//   - Redis.Port is a pointer fed via a refresh site (refresh-derived retype);
//   - ACLRule.ResourceNames is a value fed only via a build site that flows into
//     the identically named SDK struct field (build-name-derived retype).
package shared

// Redis mirrors a pointer-typed SDK field.
type Redis struct {
	// A port number, or a referenceable value.
	Port any `json:"port,omitempty"`
}

// GetPort is the SDK getter for the pointer field.
func (r *Redis) GetPort() any {
	if r == nil {
		return nil
	}
	return r.Port
}

// ACLRule mirrors a value-typed SDK field whose name matches the model struct.
type ACLRule struct {
	// If any of these entries match, the resource name matches for this rule.
	ResourceNames any `json:"resource_names"`
}

// GetResourceNames is the SDK getter for the value field.
func (r *ACLRule) GetResourceNames() any {
	if r == nil {
		return ""
	}
	return r.ResourceNames
}
