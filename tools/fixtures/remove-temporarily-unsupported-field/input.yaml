openapi: 3.0.0
info:
  title: Example API
  version: 1.0.0
components:
  schemas:
    MeshHealthCheckItem:
      properties:
        spec:
          properties:
            to:
              items:
                properties:
                  default:
                    properties:
                      http:
                        properties:
                          expectedStatuses:
                            items:
                              format: int32
paths: {}
