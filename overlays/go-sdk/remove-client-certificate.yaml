overlay: 1.0.0
info:
  title: Go SDK overlay
  version: 0.0.0
actions:
  # NOTE(pmalek): this is temporary until client cert is implemented.
  - target: $["components"]["schemas"]["CreateServiceWithoutParents"]["properties"]["client_certificate"]
    remove: true
  # NOTE(pmalek): this is temporary until client cert is implemented.
  - target: $["components"]["schemas"]["CreateUpstreamWithoutParents"]["properties"]["client_certificate"]
    remove: true
